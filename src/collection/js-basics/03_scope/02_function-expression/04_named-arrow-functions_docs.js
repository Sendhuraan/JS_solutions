'use strict';

(function() {

	var assert = require('chai').assert;

	describe('TODO: Named Arrow Function Expression', function() {

		/*
		
		var getId = person => person.id;
		var ids = people.map(getId);

		var getDataFrom = person => getData(person.id);

		getPerson()
		.then(getDataFrom)
		.then(renderData);
		
		*/

		it('TODO: Named Arrow Function Expression', function() {
			assert.equal('TODO', 'TODO');
		});

	});
	
})();
