'use strict';

(function() {

	var assert = require('chai').assert;

	describe('TODO: Anonymous Arrow Function Expression', function() {

		/*
		
		var ids = people.map(person => person.id);

		var ids = people.map(function getId(person) {
			return person.id;
		});

		getPerson()
		.then(person => getData(person.id))
		.then(renderData);

		getPerson()
		.then(function getDataFrom(person) {
			return getData(person.id);
		})
		.then(renderData);

		*/

		it('TODO: Anonymous Arrow Function Expression', function() {
			assert.equal('TODO', 'TODO');
		});

	});
	
})();
