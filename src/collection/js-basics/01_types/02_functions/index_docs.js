(function() {

	var assert = require('chai').assert;

	describe('Primitive Type - function', function() {

		it('As mentioned earlier, given a primitive type, we have a set of expected behaviors for that type. Definitely, a function has some set of expected behaviors. Eg: We can call a function');

		it('is not actually a primitive type. It is a subtype of object primitive. It is also referred to as callable object', function() {
			
		});
		
	});
	
})();