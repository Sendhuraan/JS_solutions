'use strict';

(function() {

	var nullFunction = function(){};

	describe('Coercion Types', function() {

		it('Coercion can be categorized into explicit & implicit. More importantly, it is a style. We can\'t decide that explicit is good & implicit is bad. There are two mechanisms which can be used. The decision lies in the fact that the code should be more readable & understandable by all (or most) developers.', nullFunction);

		it('We are going to go step by step into how javascript evaluates this statement to true.', nullFunction);
	});
	
})();
